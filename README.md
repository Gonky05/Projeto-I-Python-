# Projeto-I-Python
The project implements three programmes in Python — Caranguejo, Metades and Data — which use recursive and repetitive functions to reverse sentences, split text into halves and encode letters into numbers, with an interactive menu for the user to choose the operation.
